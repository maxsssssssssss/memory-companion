# Audio Fixtures

This directory documents how to generate privacy-safe local audio fixtures.
Generated audio stays on the local machine and must not be added to Git. Only
this README is part of the repository; it contains no user recordings or user
data.

Generate or refresh the files with:

```bash
npm run fixtures:audio
npm run fixtures:audio -- --force
```
